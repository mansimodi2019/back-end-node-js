{
  "name": "nodetesttask",
  "version": "0.0.1",
  "description": "Create a node module that can assess a list of webservers and then returns a server with the lowest priority. Also create a unit test that can check if the function is running properly.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon",
    "start": "npm run build && node build/index.js",
    "build": "rimraf ./build && tsc",
    "test": "jest --watch",
    "test:coverage": "jest --coverage "
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cross-fetch": "^3.1.5",
    "jest": "^29.3.1"
  },
  "devDependencies": {
    "@jest/globals": "^29.3.1",
    "@types/jest": "^29.2.2",
    "@types/node": "^18.11.9",
    "jest-fetch-mock": "^3.0.3",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "tslib": "^2.4.1",
    "typescript": "^4.8.4"
  }
}
